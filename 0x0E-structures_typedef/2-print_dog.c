#include "dog.h"

/**
 *print_dog -  a function that prints a struct dog
 *@d: dog information
 *Return: nothing
 */

void print_dog(struct dog *d)
{
	if (d != NULL)
	{
	if
		(*d).name = name;
		(*d).age = age;
		(*d).owner = owner;
	}
}
