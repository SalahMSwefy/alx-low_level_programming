#include "main.h"
/**
 *_abs - print the absolute value of n
 *@n: is an integer
 *Return: return 0 
 */
int _abs(int n)
{
	
     if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (0);
	}
}
