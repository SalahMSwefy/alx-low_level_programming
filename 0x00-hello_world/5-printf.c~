#include <stdio.h>
/**
 * main - main block
 * Return: 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\"Programming is like building a multilingual puzzle");
return (0);
}
